<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smu.oss14.bb.weather_app">

    <!-- 일반 권한 설정 (1 : 위치 권한) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AreaComp"/>
        <activity android:name=".SettingArea"/>
        <activity android:name=".SettingActivity"/>
        <activity android:name=".SettingTempScale"/>
        <activity android:name=".SettingCurLocation"/>
        <activity android:name=".SettingAreaMenu"/>
    </application>

</manifest>